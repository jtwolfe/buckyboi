# Historical Python port

Pygame 800×600 window implementing [SPEC.md](../SPEC.md). **Not** the
desktop overlay and **not** the identity stack. The product is
`../rust` (`buckyboi`).

```bash
python3 test_sim.py
```
